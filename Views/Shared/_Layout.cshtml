<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - BookManager</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/css/site.css" rel="stylesheet" />

    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            display: flex;
            flex-direction: column;
            background-image: url('/Assets/Pics/MainBackground.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-color: #121212;
            color: #ffffff;
        }

        main {
            flex: 1; /* ← macht den Content dehnbar */
        }

        footer {
            flex-shrink: 0;
        }
        table {
            background-color: #1e1e1e;
            color: white;
        }

        th, td {
            background-color: #2c2c2c !important;
            color: white !important;
        }

        thead.table-dark {
            background-color: #000 !important;
        }

        .table-hover tbody tr:hover {
            background-color: navajowhite;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm border-bottom">
        <div class="container">
            <a class="navbar-brand" href="/">📚 BookManager</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Buecher" asp-action="Index">Bücher</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Buecher" asp-action="Create">Neues Buch</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Buecher" asp-action="DontRead">Gelesen</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main content -->
    <main class="container py-4">
        @RenderBody()
    </main>

    <!-- Sticky Footer -->
    <footer class="bg-dark text-center py-3 border-top">
        <div class="container">
            <small class="text-white">&copy; @DateTime.Now.Year - BookManager by Marcel</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
